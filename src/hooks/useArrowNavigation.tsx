/* ---------------------------------
useArrowNavigation
--------------------------------- */

import { navigate } from "gatsby"
import React, { ReactElement, useEffect, useState } from "react"

interface IOwnProps {
  location: string
}

export default function useArrowNavigation(props: IOwnProps): any {
  return null
}
